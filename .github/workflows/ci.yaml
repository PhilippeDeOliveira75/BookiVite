name: Integration continue

on:
  # push:
  #   branches: ["main"]
  workflow_call

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run test
        run: echo Hello test

  routage_main:
    needs: [hello]
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: back

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 20.x

      - name: Install Dep
        run: npm i

      - name: Test Main Router
        run: npm run test

  deploiement:
    needs: [routage_main]
    runs-on: ubuntu-latest

    steps:
      - name: Joie
        run: echo Gaite Bonheur
      